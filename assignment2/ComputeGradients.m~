function [dW1, dW2, db1, db2] = ComputeGradients(X,Y,W,b)

[P,H] = EvalClassifier(X,W,b);

[~,n] = size(Y);

G = -(Y-P);

db2 = (1/n)*G*ones(n,1);
dW2 = (1/n)*G*H';

G = W2'*G;
Ind = H > 0;
G = G.*Ind;

dW1 = (1/n)*G*X';
db1 = (1/n)*G*ones(n,1);

end

